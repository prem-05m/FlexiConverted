import 'database_service.dart';

/// Stub implementation — never called. Used as the default for conditional import.
DatabaseService createPlatformDatabase() {
  throw UnsupportedError('Cannot create database without a platform implementation');
}
